import React from 'react';

const Button=()=>{
    return(
        <div>
            <button type="button" className="btn btn-outline-primary">+ Add Appointment</button>
        </div>
    );
}
export default Button;